version: '3'
services:
  sofi:
    image: sofi:latest
    ports:
      - '8080:8080'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../.:/root/sofi
    environment:
      APP_CONTAINER_PATH: /root/sofi